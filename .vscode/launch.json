{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Extension",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}/extension",
        "${workspaceFolder}/sidecar/testdata/vaults/example"
      ],
      "outFiles": ["${workspaceFolder}/extension/out/**/*.js"],
      "preLaunchTask": "build",
      "env": {
        "MUNINN_SIDECAR_PATH": "${workspaceFolder}/sidecar/dist/muninn-sidecar"
      }
    }
  ]
}
